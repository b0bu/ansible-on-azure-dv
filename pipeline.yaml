trigger:
  branches:
    include:
      - main
  paths:
    include:
      - "*"
pr:
  branches:
    include: 
      - main
  paths:
    include:
      - "*"

pool:
  vmImage: "ubuntu-18.04"

stages:
  - stage: "yolo"
    jobs:
      - job: "yolo job"
        steps:
          - task: Bash@3
            displayName: "yolo display name"
            inputs:
              script: |
                echo "hello, world!"

